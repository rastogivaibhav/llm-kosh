{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "LlmKosh Boot Contract",
  "description": "Schema defining the requirements of the 01_BOOT.md file (informative, usually a markdown document rather than JSON).",
  "type": "object",
  "properties": {
    "required_sections": {
      "type": "array",
      "items": { "type": "string" }
    },
    "rules": {
      "type": "array",
      "items": { "type": "string" }
    }
  }
}
